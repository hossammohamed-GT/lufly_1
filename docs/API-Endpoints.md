# API Reference

> Auto-generated by `php cli docs:api`. Do not edit manually.

All responses follow the platform envelope:

```json
{ "success": true, "message": "", "data": {} }
{ "success": false, "message": "", "errors": {} }
```

| # | Method | Endpoint | Summary |
| --- | --- | --- | --- |
| 1 | `GET/HEAD` | `/api/health` | Health check endpoint. |
| 2 | `GET/HEAD` | `/api/locales` | List supported locales. |
| 3 | `POST` | `/api/auth/login` | Authenticate with email + password (session based). |
| 4 | `POST` | `/api/auth/logout` | Log out the current session. |
| 5 | `GET/HEAD` | `/api/auth/me` | Return the authenticated user profile. |
| 6 | `GET/HEAD` | `/api/products/` | List products with pagination, search and locale. |
| 7 | `GET/HEAD` | `/api/products/{id}` | Fetch one product with all its translations. |
| 8 | `POST` | `/api/products/` | Create a product with translations. |
| 9 | `PUT` | `/api/products/{id}` | Update a product and/or its translations. |
| 10 | `DELETE` | `/api/products/{id}` | Soft-delete a product. |
| 11 | `GET/HEAD` | `/api/media/` | List media library entries. |
| 12 | `POST` | `/api/media/` | Upload a file (multipart/form-data, field: file). |
| 13 | `DELETE` | `/api/media/{id}` | Delete a media entry and its file. |
| 14 | `GET/HEAD` | `/api/notifications/` | List unread notifications for the current user. |
| 15 | `POST` | `/api/notifications/{id}/read` | Mark one notification as read. |
| 16 | `POST` | `/api/notifications/read-all` | Mark all notifications as read. |

## Endpoints

### `GET / HEAD /api/health`

Health check endpoint.

**Example response**

```json
{
    "success": true,
    "data": {
        "status": "ok",
        "app": "Lufly Platform"
    }
}
```

**Error codes:** `401` unauthenticated · `403` forbidden · `422` validation_failed · `404` not_found

### `GET / HEAD /api/locales`

List supported locales.

**Example response**

```json
{
    "success": true,
    "data": {
        "current": "en",
        "supported": {
            "en": "English"
        }
    }
}
```

**Error codes:** `401` unauthenticated · `403` forbidden · `422` validation_failed · `404` not_found

### `POST /api/auth/login`

Authenticate with email + password (session based).

**Validation rules**

| Field | Rules |
| --- | --- |
| `email` | `required|email` |
| `password` | `required|string` |

**Example response**

```json
{
    "success": true,
    "message": "Welcome back.",
    "data": {
        "user": {
            "id": 1,
            "name": "Admin",
            "email": "admin@lufly.test"
        }
    }
}
```

**Error codes:** `401` unauthenticated · `403` forbidden · `422` validation_failed · `404` not_found

### `POST /api/auth/logout`

Log out the current session.

**Example response**

```json
{
    "success": true,
    "message": "Logged out.",
    "data": {}
}
```

**Error codes:** `401` unauthenticated · `403` forbidden · `422` validation_failed · `404` not_found

### `GET / HEAD /api/auth/me`

Return the authenticated user profile.

**Example response**

```json
{
    "success": true,
    "data": {
        "user": {
            "id": 1,
            "name": "Admin",
            "email": "admin@lufly.test"
        }
    }
}
```

**Error codes:** `401` unauthenticated · `403` forbidden · `422` validation_failed · `404` not_found

### `GET / HEAD /api/products/`

List products with pagination, search and locale.

**Example response**

```json
{
    "success": true,
    "data": [
        {
            "id": 1,
            "name": "Sample product",
            "price": "19.90"
        }
    ],
    "meta": {
        "page": 1,
        "per_page": 10,
        "total": 1,
        "last_page": 1
    }
}
```

**Error codes:** `401` unauthenticated · `403` forbidden · `422` validation_failed · `404` not_found

### `GET / HEAD /api/products/{id}`

Fetch one product with all its translations.

**Example response**

```json
{
    "success": true,
    "data": {
        "product": {
            "id": 1,
            "name": "Sample product"
        }
    }
}
```

**Error codes:** `401` unauthenticated · `403` forbidden · `422` validation_failed · `404` not_found

### `POST /api/products/`

Create a product with translations.

**Validation rules**

| Field | Rules |
| --- | --- |
| `sku` | `required|string|max:100|unique:products,sku` |
| `price` | `required|numeric|min:0` |
| `status` | `required|in:active,inactive,draft` |
| `translations` | `required|array` |

**Example response**

```json
{
    "success": true,
    "message": "Saved.",
    "data": {
        "product": {
            "id": 2,
            "sku": "SKU-2"
        }
    }
}
```

**Error codes:** `401` unauthenticated · `403` forbidden · `422` validation_failed · `404` not_found

### `PUT /api/products/{id}`

Update a product and/or its translations.

**Validation rules**

| Field | Rules |
| --- | --- |
| `sku` | `nullable|string|max:100` |
| `price` | `nullable|numeric|min:0` |
| `status` | `nullable|in:active,inactive,draft` |
| `translations` | `nullable|array` |

**Example response**

```json
{
    "success": true,
    "message": "Saved.",
    "data": {
        "product": {
            "id": 1,
            "sku": "SKU-1"
        }
    }
}
```

**Error codes:** `401` unauthenticated · `403` forbidden · `422` validation_failed · `404` not_found

### `DELETE /api/products/{id}`

Soft-delete a product.

**Example response**

```json
{
    "success": true,
    "message": "Deleted.",
    "data": {}
}
```

**Error codes:** `401` unauthenticated · `403` forbidden · `422` validation_failed · `404` not_found

### `GET / HEAD /api/media/`

List media library entries.

**Example response**

```json
{
    "success": true,
    "data": {
        "media": [
            {
                "id": 1,
                "filename": "abc.png",
                "mime_type": "image/png"
            }
        ]
    }
}
```

**Error codes:** `401` unauthenticated · `403` forbidden · `422` validation_failed · `404` not_found

### `POST /api/media/`

Upload a file (multipart/form-data, field: file).

**Validation rules**

| Field | Rules |
| --- | --- |
| `file` | `required|file` |
| `collection` | `nullable|string|max:50` |

**Example response**

```json
{
    "success": true,
    "message": "Uploaded.",
    "data": {
        "media": {
            "id": 1,
            "filename": "abc.png"
        }
    }
}
```

**Error codes:** `401` unauthenticated · `403` forbidden · `422` validation_failed · `404` not_found

### `DELETE /api/media/{id}`

Delete a media entry and its file.

**Example response**

```json
{
    "success": true,
    "message": "Deleted.",
    "data": {}
}
```

**Error codes:** `401` unauthenticated · `403` forbidden · `422` validation_failed · `404` not_found

### `GET / HEAD /api/notifications/`

List unread notifications for the current user.

**Example response**

```json
{
    "success": true,
    "data": {
        "notifications": []
    }
}
```

**Error codes:** `401` unauthenticated · `403` forbidden · `422` validation_failed · `404` not_found

### `POST /api/notifications/{id}/read`

Mark one notification as read.

**Example response**

```json
{
    "success": true,
    "data": {}
}
```

**Error codes:** `401` unauthenticated · `403` forbidden · `422` validation_failed · `404` not_found

### `POST /api/notifications/read-all`

Mark all notifications as read.

**Example response**

```json
{
    "success": true,
    "data": {}
}
```

**Error codes:** `401` unauthenticated · `403` forbidden · `422` validation_failed · `404` not_found
